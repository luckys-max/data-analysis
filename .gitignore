# Intentionally empty to allow uploading the whole repository
# Previously ignored patterns removed so all files can be added and pushed.

# If you later want to ignore specific files, add patterns below.
